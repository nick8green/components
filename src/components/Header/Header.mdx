import { Canvas, Meta } from "@storybook/blocks";
import * as Header from "./Header.stories.tsx";

<Meta of={Header} title="Header" />

# Header

Header component for a website.

#### Example

<Canvas of={Header.Default} />

## Arguments

TBC

## Usage

```ts
import { Header } from "@nick8green/components";

const Example = () => {
  return (
      <Header
      />
  );
};

export default Example;
```
