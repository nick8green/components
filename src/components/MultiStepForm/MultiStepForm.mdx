import { Canvas, Meta } from "@storybook/blocks";
import * as MultiStepForm from "./MultiStepForm.stories.tsx";

<Meta of={MultiStepForm} title="MultiStepForm" />

# MultiStepForm

MultiStepForm component.

#### Example

<Canvas of={MultiStepForm.Default} />

## Usage

```ts
import {MultiStepForm} from "@nick8green/components";

const Example = () => {
  return (
      <MultiStepForm />
  );
};

export default Example;
```

#### Arguments

TBD
